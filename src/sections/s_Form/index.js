import { C_Form } from '../../components/c_Form';
import classes from './style.module.scss';

export const S_Form = ({}) => {   
    return (
        <div className={classes.root}>
          <C_Form />
        </div>
    )
}